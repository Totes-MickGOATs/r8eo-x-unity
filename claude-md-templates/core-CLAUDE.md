# core/

Core framework classes: system manifests, project registry, shared utilities.

## System Registry

Every game system has a `SystemManifest` in `resources/manifests/`.
The `ProjectRegistry` aggregates all manifests for querying.

## Relevant Skills

- `.agents/skills/godot-resource-data-patterns/SKILL.md`
