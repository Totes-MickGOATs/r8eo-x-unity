# scripts/tools/

Build utilities, asset generation, and benchmarking scripts.

## Python Scripts (run with `uv run python <script>`)

| File | Role |
|------|------|
<!-- Add your scripts here -->

## Notes

- Python scripts use `uv` package manager (`.venv/` at project root)
- Run: `uv run python scripts/tools/<script>.py`
