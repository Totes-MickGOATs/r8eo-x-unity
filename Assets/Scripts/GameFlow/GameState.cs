namespace R8EOX.GameFlow
{
    /// <summary>
    /// All possible states in the game flow lifecycle.
    /// </summary>
    public enum GameState
    {
        Boot,
        Splash,
        MainMenu,
        ModeSelect,
        CarSelect,
        TrackSelect,
        Loading,
        Playing,
        Paused,
        Results
    }
}
