#!/usr/bin/env bash
# Engine-specific hook: no-op stub. Override with engine implementation if needed.
# This file is sourced by lint-session-stop.sh when it exists.
# Replace with engine-specific lint checks (e.g., Unity compilation errors,
# Godot parse errors, shader validation, etc.) as required by the project's engine setup.
