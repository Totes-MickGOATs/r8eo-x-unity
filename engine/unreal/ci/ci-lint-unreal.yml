name: Lint — Unreal

on:
  workflow_call:

jobs:
  cpp-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: C++ lint (stub)
        run: echo "C++ linting not yet configured. Add clang-tidy or cppcheck."
